[package]
name    = "omega-protocol"
version = "0.1.0"
edition.workspace = true

[lints]
workspace = true

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
insta      = { version = "1", features = ["json", "redactions"] }
