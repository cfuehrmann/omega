{
  "name": "omega",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/ui-raw.ts",
    "login": "bun run src/login.ts",
    "check": "bun test && bunx knip"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0"
  }
}
